<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Tema" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="colorPrimary">@color/ligh_green</item>
        <item name="colorPrimaryVariant">@color/green</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="colorAccent">@color/yellow</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/yellow</item>
        <item name="colorSecondaryVariant">@color/orange</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- Coloca os icons da status bar a preto -->
         <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Define o estilo da barra do header -->
    <style name="TitleLine">
        <item name="android:layout_height">3dp</item>
        <item name="android:backgroundTint">@color/yellow</item>
        <item name="android:fontFamily">@font/raul_lino_fonte</item>
    </style>

    <!-- Define o estilo da barra de ativos na navbar -->
    <style name="ActiveNavigationStyle">
        <item name="android:layout_height">3dp</item>
        <item name="android:backgroundTint">@color/yellow</item>
    </style>

    <!-- Define o estilo do texto -->
    <style name="BodyText">
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/fira_sans</item>
    </style>

</resources>
